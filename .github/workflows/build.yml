name: Build and Release

on:
  push:
    branches:
      - "Neo/1.21.1"

jobs:
  build:
    runs-on: ubuntu-latest

    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: "21"
          distribution: "temurin"

      - name: Read Version
        id: gradle_props
        run: |
          VERSION=$(grep '^mod_version=' gradle.properties | cut -d'=' -f2)
          HILT_VERSION=$(grep '^hilt_version=' gradle.properties | cut -d'=' -f2)
          echo "mod_version=$VERSION" >> $GITHUB_OUTPUT
          echo "hilt_version=$HILT_VERSION" >> $GITHUB_OUTPUT
          echo "release_version=${VERSION}-build.${{ github.run_number }}" >> $GITHUB_OUTPUT

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Make gradlew executable
        run: chmod +x gradlew

      - name: Download latest Hilt-Revived build
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          mkdir -p libs hilt-dl
          # Grab the latest published Hilt-Revived release jars
          gh release download \
            --repo KingNadienDev/Hilt-Revived \
            --dir hilt-dl \
            --pattern 'Hilt-*.jar' \
            --clobber
          # Pick the main jar (exclude sources/javadoc/all classifiers)
          HILT_JAR=$(ls hilt-dl/Hilt-*.jar \
            | grep -v -- '-sources' \
            | grep -v -- '-javadoc' \
            | grep -v -- '-all' \
            | head -n1)
          if [ -z "$HILT_JAR" ]; then
            echo "::error::No Hilt jar found in the latest KingNadienDev/Hilt-Revived release"
            ls -la hilt-dl/ || true
            exit 1
          fi
          echo "Latest Hilt jar: $HILT_JAR"
          # Copy into the exact filename Tinkers' build.gradle expects
          cp "$HILT_JAR" "libs/Hilt-${{ steps.gradle_props.outputs.hilt_version }}.jar"
          echo "Placed as libs/Hilt-${{ steps.gradle_props.outputs.hilt_version }}.jar"
          ls -la libs/

      - name: Build
        run: ./gradlew build

      - name: Create Tag
        run: |
          git tag v${{ steps.gradle_props.outputs.release_version }}
          git push origin v${{ steps.gradle_props.outputs.release_version }}

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v${{ steps.gradle_props.outputs.release_version }}
          name: TinkersRevived ${{ steps.gradle_props.outputs.release_version }}
          generate_release_notes: true
          files: |
            build/libs/*.jar
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
